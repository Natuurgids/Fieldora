# Fieldora modular web architecture tasks

Status: ACTIVE — working task/dependency ledger for PR #3.

This ledger complements `WEB_DESKTOP_PARITY_PLAN.md`. Functional parity is not sufficient for completion: migrated web capabilities must be independently mountable, removable, and replaceable behind explicit contracts. Work should follow this dependency order and update the evidence/status here as slices land.

## Completion rule

A capability is modular only when its dependencies are explicit and mediated through contracts/services/events rather than another module's private DOM, mutable globals, private functions, or implementation-specific load order. Required dependencies must be declared; optional integrations must degrade safely when absent; runtime data relationships must travel through public events/contracts.

Status legend: `[ ]` missing, `[~]` partial/evidence or migration in progress, `[x]` certified.

## Foundation tasks

| ID | Status | Task | Depends on | Completion contract |
|---|---|---|---|---|
| A01 | [x] | Module contract | — | Module identity, route, capability, owned actions, provided/required contracts and lifecycle are explicit. |
| A02 | [x] | Module/capability registry | A01 | Shell discovers modules from a validated registry; duplicate ownership and unresolved requirements fail validation. |
| A03 | [~] | Shared application contracts | A01 | Auth, project context, navigation, inspector and notifications are consumed through public contracts. |
| A04 | [~] | Event/message boundary | A03 | Cross-module runtime changes use declared actions/events; producer and consumer ownership are known. |
| A05 | [~] | Service/API adapters | A03 | Presentation modules receive service/API adapters through stable interfaces; transport details are not feature globals. |
| A06 | [~] | State ownership | A03,A04 | Every shared state field has one owner; consumers use snapshots/read contracts/events and cannot mutate owner state directly. |
| A07 | [~] | DOM ownership | A01 | Each module owns a bounded render root and does not reach into another module's private DOM. |
| A08 | [~] | Shell decomposition | A01-A07 | Shell performs composition, capability gating, route/history and lifecycle only; feature business behavior is outside the shell. |

### Foundation contract certification

- A01 is certified at `98d2766f7a87b4e16930ccc342c1cbad2ee86651`. `WebModuleSpec` explicitly defines module identity, route, label, capability, owned actions, concrete dependencies, and provided/required/optional public contracts, with normalization and duplicate/overlap validation. `WebModuleAdapter` defines the shared `mount(spec)` / `unmount(spec)` lifecycle contract, `WebModuleRuntime` owns lifecycle states, ordering and failure isolation, and the production browser shell emits `fieldora:module-unmount` before `fieldora:module-mount` while carrying the public module spec in each lifecycle event. Exact-head modular-shell run #390 is green and exercises the module-contract/runtime test suites.
- A02 is certified against the same implementation head. `foundation_registry()` validates concrete dependencies and required contract providers before the shell consumes the registry. Registry registration rejects duplicate module IDs, routes, owned actions and contract providers; validation rejects unknown dependencies and unresolved required contracts. Browser-manifest tests prove the shell module set and provided/required/optional contract metadata match the typed registry, and a custom registry produces a custom bootstrap rather than relying on a hard-coded module list.

## Projects slice — Project list and project context

| ID | Status | Task | Depends on | Completion contract |
|---|---|---|---|---|
| P01 | [x] | Projects capability object | A01-A08 | Projects can mount/unmount behind shell contract without owning unrelated workspaces. |
| P02 | [x] | Project list provider/object | P01,A05,A06 | Accessible projects have one owner/provider; consumers do not read ambient `projects`. |
| P03 | [x] | Project context object | P02,A04,A06 | Selected project is validated against accessible list before publication; inaccessible/stale IDs do not mutate context. |
| P04 | [x] | Project scope/filter object | P02 | `My work` and `All accessible` are explicit scope semantics; zero `My work` matches remains zero. |
| P05 | [x] | Project hierarchy/tree object | P03,A05 | Hierarchy consumes project-context and work-data contracts; it does not own project selection. |
| P06 | [x] | Project inspector object | P03,P05 | Inspector consumes selected-record contract and can be replaced without changing hierarchy/context. |
| P07 | [x] | Project work-data adapters | P03,A05 | Phases/tasks/sprints/allocations/evidence are accessed behind declared service contracts. |
| P08 | [x] | Remove mutable global Projects coupling | P02-P07 | `projects`, `selectedProject`, direct feature globals and equivalent ambient state are no longer module integration APIs. |
| P09 | [x] | Retire legacy Projects wiring | P01-P08 | Final `/app.js` contains one owner per migrated responsibility and no competing legacy listeners. |
| P10 | [x] | Module removal test | P01-P09 | Remove/disable Projects module and prove shell plus unrelated modules still boot/function. |
| P11 | [x] | Module replacement test | P01-P09 | Substitute a minimal Projects provider implementing the same public contracts without shell/consumer changes. |
| P12 | [x] | Dependency contract tests | A01-P11 | Duplicate ownership, missing providers and undeclared concrete coupling are rejected where mechanically testable. |
| P13 | [x] | Browser behavior certification | P02-P07 | Chromium/Firefox/WebKit cover list, context switch, strict empty `My work`, stale/inaccessible selection, error and recovery. |
| P14 | [x] | API/security certification | P02,P07 | Accessible-list filtering, cross-org exclusion and scoped authorization are proven independently of UI hiding. |
| P15 | [x] | Persistence/domain proof | P07 | Visible project result traces through governed API/application contract to authoritative persisted/domain state. |
| P16 | [x] | Eight-link certification chain | P01-P15 | capability → visible entry point → input validation → owned wiring → API/application call → persisted/domain outcome → visible result → audit/test evidence. |
| P17 | [x] | Dependency graph maintained | A01-P16 | Required, optional and provided contracts are represented in registry/manifest and this ledger stays current. |
| P18 | [x] | Certify Project list/context | P01-P17 | Iteration 6 item may move from `[~]` only when behavior and removal/replacement boundaries are proven at one exact head. |

## Current exact-head certification at `5edd075ac90c8f474703f9089caac81e292fa9f2`

- P01 is certified. The Projects-free registry/runtime tests remove `projects.core` and every Project-contract consumer while retaining Home, Library, Observations, Knowledge and Administration with valid shell/runtime manifests. The Chromium removal test boots that composition, confirms Project routes/contracts/surfaces are absent, and exercises unrelated Library navigation/filtering successfully.
- P02 is certified. `projects.list.read` has one Projects-owned runtime provider with immutable snapshots, refresh deduplication and loaded-state reporting. Project Core, Portfolio and managed compatibility consumers resolve the public list contract rather than an ambient Project-list integration API; final managed selector refresh does not mutate ambient `projects`.
- P03 is certified. `projects.context.select` owns selected-Project state and validates requested IDs against `projects.list.read` before publication. WEB-056 run #571 proves across Chromium, Firefox and WebKit that an initial Project-list failure leaves context empty, recovery selects an accessible Project, normal tree selection changes context, stale/inaccessible selection returns `false` without changing the active Project, strict `My work` can be empty, and `All accessible` restores rows without corrupting context.
- P04 is certified. Project Core implements strict `My work` and `All accessible` scope semantics, keeps zero `My work` matches at zero, and the managed Chromium regression types into `#project-tree-filter`, observes the filtered Project row set, clears the filter, and observes restoration.
- P08 is certified. Managed Project integration uses `projects.list.read`, `projects.context.select`, `projects.toolbar.extend`, `projects.selected-record.select`, `projects.work-data.service`, and `projects.evidence.service` instead of `window.FieldoraProjects`, `window.FieldoraProjectContext`, `selectedProject`, or an ambient `projects` mirror. The final context provider refreshes surviving legacy selectors from the canonical list contract without mutating ambient Project state; WEB-056 uses the public context contract directly.
- P09 is certified. Final managed composition strips legacy Project hierarchy/Portfolio/navigation behavior, the old lifecycle patch, competing Research Project-list/save wiring, the legacy `saveWorkItem()` editor and save handler, its `work-project` selector feed, and the orphaned Project Core `#work-project` context mirror once their modular owners are present. Owner-gating tests preserve fallback behavior when a modular owner is intentionally absent and preserve neighboring non-Project compatibility.
- P17 is certified. `test_modular_shell_contract_manifest.py` proves the browser manifest matches the typed registry for every module's provided, required, and optional contracts; `test_web_module_contract_runtime.py` proves the runtime manifest exposes the same Project contract graph and enforces provider identity, duplicate registration, and required-contract availability. This ledger is synchronized with the certified Project graph.
- P18 is certified at this exact head. Modular-shell run #389 checked out and verified `5edd075ac90c8f474703f9089caac81e292fa9f2`, passed all Ruff gates and all 159 contract/runtime/browser tests. That run executed the Projects-free browser removal test and the browser replacement-provider test in which `projects.replacement` provides the public contracts and the unchanged Portfolio consumer renders and selects replacement data. WEB-056 run #571 provides same-head cross-browser list/context behavior, project-parity run #829 is green, and the complete visible exact-head workflow wave finished successfully.

## Historical dependency findings at audit head `eeff9e6edc6fc2b5fe96419d40a13ca938e2a4ee`

The findings below are retained as audit history. Where they describe debt later closed by P01-P18, the current exact-head certification section above supersedes them.

- P10 is certified at exact head `0ad0fc1127edf9dd520250e4f633f60de8e40734`. The explicit Projects-free composition removes `projects.core`, `portfolio`, `capacity`, `research.dossiers` and `dossiers.workspace` from the shell and contract manifests, removes their foundation-owned navigation/page DOM surfaces, leaves their public Project contracts without providers, and still boots Chromium with unrelated Library navigation and media filtering functional. `Fieldora modular shell certification` run #329 and the full visible exact-head workflow set completed successfully for that head.
- P11 is certified at exact head `a2f7d9c96f23a166a84a19efad690ae9c9eb3366`. The explicit registry substitutes `projects.replacement` for `projects.core` while declaring and registering the same `projects.list.read`, `projects.context.select` and `projects.toolbar.extend` contracts. The unchanged Portfolio consumer has no unresolved required contracts, renders replacement-provided Project data on the existing Projects & Portfolio surface, and routes normal row selection through the replacement context contract. `Fieldora modular shell certification` run #332 and the full visible exact-head workflow set completed successfully for that head.
- P12 is certified at exact head `59c11c74814fa8e84e3a2974864e6cb48c3f94f7`. Registry tests reject duplicate route, action and contract ownership, unknown concrete module dependencies, and missing required contract providers. Replacement-provider tests prove Portfolio, Capacity, Research and Dossiers bind through declared Projects contracts with no concrete `projects.core` dependency, while the Portfolio module source test rejects ambient `window.projects`, `window.openProject` and `window.loadPortfolio` coupling. `Fieldora modular shell certification` run #333 and the full visible exact-head workflow set completed successfully for that head.
- P13 is certified at exact head `7f37fad67dd64544e1421fa6899413b260896ae3`. WEB-056 browser certification covers Chromium, Firefox and WebKit for Project list recovery after a 503, context changes through the delegated Project tree, rejection of stale/inaccessible Project selection without losing the active context, strict-empty My work scope, restoration to All accessible, and recovery to the expected Project rows. `Fieldora WEB-056 cross-browser workflow certification` run #521 and the full visible exact-head workflow set completed successfully for that head.
- P14 is certified at exact head `b6e7ae2b20ad15cd3b7ee987073fd94c2a5b97a9`. The managed Projects GET first scopes persisted Project candidates by the authenticated identity's organization and then applies a `view` PBAC decision for each Project before returning it. PostgreSQL integration tests prove organization-isolated Project lists and reject cross-organization mutation, while managed API tests prove denied create/edit operations persist nothing and record organization/project-scoped authorization requests. `Fieldora project parity certification` run #780 and the full visible exact-head workflow set completed successfully for that head.
- P15 is certified at exact head `01e2d5233a48bbad7f9baf40a23e0b86ece2f70f`. The real managed-browser creation path clicks the Project editor save control, posts to `/api/v1/projects`, receives the server-canonical Project ID, waits for that same ID to render in the Project tree, and then reads the authoritative Project back from `PostgresProjectManagementService` with matching name, description, dates, budget, currency and owner. The same test verifies the persisted admin membership, default statuses and `project.created` activity record, proving the visible result is backed by governed persisted/domain state rather than a UI-only snapshot. `Fieldora project parity certification` run #781 and the full visible exact-head workflow set completed successfully for that head.
- P16 is certified at exact head `eeff9e6edc6fc2b5fe96419d40a13ca938e2a4ee`. The registry declares `projects.core` as owner of `/projects` and `projects.create`; the Project creation module renders the Add project editor, validates required name, date order and non-negative budget, owns the save wiring, posts through the governed `/api/v1/projects` API, refreshes through `projects.list.read` and `projects.context.select`, and the managed-browser integration test proves the server-canonical persisted Project returns to the visible Project tree with matching authoritative Postgres/domain state. `Fieldora project parity certification` run #782, `Fieldora modular shell certification` run #342, and the full visible exact-head workflow set completed successfully for that head.
- P07 is certified at exact head `d9693be7042a8f0a2c1625604074d44eb7e845b6`. `projects.core` declares and runtime-registers `projects.work-data.service` and `projects.evidence.service`; the work-data provider owns governed phases/tasks/sprints/allocations load/create transport and the evidence provider owns Project/Library evidence load/link transport. Project Core and both work/evidence action adapters resolve those services rather than issuing work/evidence transport directly, while provider-order tests prove the contract runtime precedes both service providers and both precede the Project-list provider. `Fieldora modular shell certification` run #350 passed all Ruff stages and the expanded pytest set (`142 passed`), and every visible exact-head workflow completed successfully for that head. P08 remained open at that head and was certified later in the current section above.
- P05 is certified at exact head `0fdac0daef32462eb2765d78aff72fbb3e6350d8`. `projects.context.select` owns validated selected-Project state against `projects.list.read`, reconciles stale selection on canonical list changes, and publishes Project-context changes. Project Core/hierarchy resolves that contract for tree selection and mirrors context only for rendering/work/evidence loading; it no longer validates or publishes canonical Project selection itself. Work hierarchy continues to consume `projects.work-data.service`. At that head the surviving `window.FieldoraProjects.selectProject/currentProject` compatibility surface remained explicit P08 debt; that façade was retired later under P08. `Fieldora modular shell certification` run #358 passed all Ruff stages and the expanded pytest set (`149 passed`); `Fieldora WEB-056 cross-browser workflow certification` run #540 passed Ruff and all six Chromium/Firefox/WebKit cases (`6 passed`), and every visible exact-head workflow completed successfully for that head.
- P06 is certified at exact head `536cb352b7a582c0607e4c42a521d29078dbda0b`. `projects.core` declares and runtime-registers `projects.selected-record.select`; a dedicated selected-record provider owns immutable selected-record snapshots and publishes `fieldora:project-selected-record-changed`. Project Core/hierarchy resolves that contract and publishes Project/work selections through it while no longer owning inspector DOM, state, tab selection, or rendering. The production inspector is a separate consumer of the selected-record contract/event. Focused Chromium replacement coverage omits the production inspector entirely and proves an unchanged hierarchy/context boundary can drive a replacement consumer through the selected-record contract, including frozen selection and record snapshots. `Fieldora modular shell certification` run #371 passed all Ruff stages and the expanded pytest suite (`154 passed`); `Fieldora WEB-056 cross-browser workflow certification` run #553, `WEB-032 managed Project hierarchy certification` run #527, and `Fieldora project parity certification` run #811 passed at the same exact head, and every visible exact-head workflow completed successfully. P08 ambient/global coupling and P09 legacy retirement were out of scope for that certification head and were closed later.
- The registry and browser runtime now distinguish concrete module dependencies from public provided/required contracts. `projects.core` provides `projects.list.read`, `projects.context.select` and `projects.toolbar.extend`; Portfolio requires list/context contracts rather than the concrete Projects module ID.
- `projects.list.read` has one Projects-owned provider with immutable snapshots, refresh deduplication and loaded-state reporting. Projects/Core and Portfolio consume that contract instead of reading ambient `projects`/`window.projects`.
- `web_capabilities.py` routes modular initial Project loading through `projects.list.read`. When the modular contract runtime exists, missing `projects.list.read` now fails closed instead of falling back to ambient `projects` or directly fetching `/api/v1/projects`; the direct API bootstrap remains only for the non-modular legacy runtime.
- Home's managed zero-trust renderer resolves `projects.list.read` and renders its visible Project metric/cards from a copied contract snapshot. Its `projectItems()` helper returns an empty managed result when the contract runtime exists without the list contract, retaining ambient `projects` only for the non-modular compatibility runtime. Focused zero-trust tests now prove that fail-closed boundary.
- Project Creation and Project Lifecycle require `projects.list.read` for refresh and no longer copy refreshed snapshots into ambient `projects` or call `projectOptions()` themselves. The compatibility boundary refreshes surviving selector-backed consumers from the canonical list contract without mutating ambient Project state.
- Managed zero-trust `loadBase()` records whether `projects.list.read.refresh()` completed and skips its own legacy `projectOptions()` call on that successful canonical contract path. The provider's `fieldora:project-list-changed` event therefore leaves selector refresh at the compatibility boundary without an ambient Project-list mirror. Denied Projects, missing list contract under the contract runtime, and the no-contract-runtime direct `/api/v1/projects` fallback still call `projectOptions()` so legacy selectors can be cleared or populated. Focused zero-trust regression coverage locks in this path distinction.
- `projects.context.select` has a Projects-owned provider around the existing public Projects module API. Portfolio, Capacity, Research and Dossiers consume that contract for project-context interactions instead of reading the Projects implementation directly.
- `projects.toolbar.extend` is a Projects-owned cockpit extension contract. Capacity and Research register their cross-module entry points through it instead of querying the Projects cockpit DOM or following `projects.core` mount/unmount lifecycle.
- Capacity and Research declare no concrete dependency on `projects.core`; both require `projects.context.select` and `projects.toolbar.extend`. Focused registry tests prove each consumer can validate against a replacement Projects provider implementing those contracts. This remains meaningful P12 evidence; P11 runtime replacement is certified independently above.
- Capacity is split between two modular owners: `capacity_module_web.py` owns selected-Project allocations, while `capacity_availability_module_web.py` owns schedules, absences and organisational obligations. Both consume canonical Project context without `projects.list.read` or concrete `projects.core` coupling. Under the two-owner gate, `capacity_legacy_retirement_web.py` removes legacy `loadCapacity()`, the Capacity `showPage` load invocation, refresh wiring and `capacity-project` option population; `capacity_availability_module_web.py` removes legacy `saveCapacity()` and its save wiring. The Projects→Capacity adapter now passes `projects.context.select` directly to `window.FieldoraCapacity.openProject(pid)` and no longer reads or writes `#capacity-project`. Focused tests plus real bundled `resources/server_web/app.js` composition coverage prove these retirements while preserving neighboring fallback surfaces.
- Dossier ownership is now a separate independently mountable module rather than part of `research.dossiers`: `dossiers.workspace` owns `/dossiers`, the Dossier workspace view, create action and review-create action, declares no concrete module dependency, and requires only `projects.context.select`. Focused registry tests prove it validates against a replacement provider implementing that contract without `projects.list.read`, `projects.toolbar.extend` or `projects.core`.
- `DossierModuleWebApiMixin` is composed in the managed API and the modular-shell finalizer retires the legacy `loadDossierWorkspace()` / `saveDossierWorkspace()` range plus the legacy refresh, save and workspace-list `.onclick` handlers only when both the Dossier owner marker and registered manifest entry are present. The same owner gate removes `dossier-project` from generic `projectOptions()` population while preserving `science-project` and unrelated selectors. Focused owner-gating tests plus real bundled `resources/server_web/app.js` finalization coverage prove those retirements and preservation boundaries; unrelated Research loading remains intact.
- Dossier effective Project scope is fail-closed and canonical: `dossier_module_web.py` resolves only `projects.context.select`, does not acquire `projects.list.read` or ambient `projects`, and creation stops with a visible error when the Project context is empty. The module may mirror the canonical context into `#dossier-project` for presentation, but save authority comes from the contract rather than the selector or list order.
- The temporary Dossier-specific compatibility bridge has been retired from `web_compatibility.py`. The compatibility boundary no longer resolves `projects.context.select`, touches `#dossier-project`, or listens for Project-context changes; focused regression coverage protects that separation while preserving canonical selector refresh for surviving non-Projects consumers.
- Platform AI Administration, Reference Data and Connector creation are Project-independent administration surfaces. Their save paths send the explicit platform scope `project_id:"platform"` instead of deriving a scope from `projects[0]`, removing three unrelated ambient Project-list consumers while preserving the generic resource API/PBAC project-id field. Focused regression coverage also locks the connector list renderer to its `cards(...)` contract after the adjacent rendering correction.
- Base `projectOptions()` remains in raw source for compatibility, but managed composition removes `capacity-project`, `dossier-project`, and—when the modular work-actions owner is present—`work-project`. The surviving selector-backed consumers are bounded non-Projects compatibility surfaces such as the generic Research record editor and Research science workflow.
- Portfolio no longer calls the transitional global `window.loadPortfolio`: it refreshes `projects.list.read`, owns phases/tasks/sprints loading through governed API calls, stores that work-data snapshot in its own render root, and renders its views module-locally. Focused Portfolio tests also reject `window.projects`, `window.openProject` and `window.loadPortfolio`; the exact-head modular-shell and wider workflow set are green for this slice. Portfolio still reads the ambient `window.me`, so its broader shared-service cleanup is not complete.
- P09 final-shell retirement removes the base Projects-page `showPage("projects") → loadPortfolio()` call plus legacy Portfolio refresh, scope and view-button handlers once the relevant module owners are present. Focused composition tests prove those exact legacy snippets are absent from finalized `/app.js` while their owners remain.
- The modular Project work-actions editor now covers the former legacy editor's relationship and effort fields, including phase, parent task, sprint, manual estimate and realized hours. Final managed context retirement removes `saveWorkItem()`, its `work-save` handler, its `work-project` selector feed, and the obsolete Project Core `#work-project` context mirror when the modular work-actions owner is present.
- Project Core no longer writes the transitional ambient `selectedProject` global or the retired `#work-project` context mirror in final managed composition.
- Navigation and desktop-cockpit compatibility patches may still contain `loadPortfolio` references in raw source, but those Portfolio/Projects-owned compatibility ranges are stripped by the finalizer when the corresponding owner markers are present. Raw source presence is not evidence of a competing final-shell owner.
- Research no longer consumes the ambient Project list as an active managed integration surface: when the Research owner marker is present, finalization removes the base `cards("project-list",projects,...)` renderer and its `#project-list` click handler. The surviving Home click handler reaches the managed rebound `openProject`, whose adapter selects through `projects.context.select` before invoking the legacy detail renderer for presentation.
- The Research adapter, Research record bridge, export path and record-editor prefill do not read or write `selectedProject`. Finalization removes the legacy `selectedProject=id` assignment from the compatibility `openProject(id)` renderer while preserving that renderer's visible `#project-detail` presentation. Focused modular-shell tests prove this retirement is bounded.
- P08 is closed: final managed Project integration no longer uses an ambient `projects` mirror, `selectedProject`, `window.FieldoraProjects`, or `window.FieldoraProjectContext` as module integration APIs. Surviving selector-backed compatibility is refreshed from `projects.list.read`, and Research/Capacity/Dossier integrations use declared Project contracts.
- Research's bounded Projects integration intentionally requires only `projects.context.select` and `projects.toolbar.extend`; it does not require `projects.list.read`. The legacy Research list surface is retired under the owner marker rather than being migrated into a new Research list dependency.
- Finalized-shell tracing is complete for the migrated Projects responsibilities covered by P08/P09. Raw legacy source may remain for non-modular fallback or bounded neighboring domains, but owner-gated final managed composition contains the certified modular Project owners rather than competing legacy Project wiring.
- Qt Project Management obtains accessible projects through `WorkspaceContext.accessible_projects(...)`; its `select_project()` refreshes the accessible list and returns `False` when the requested project is absent. This supports the web stale/inaccessible-selection guard as parity behavior.
- WEB-056 now contains and passes the dedicated Projects list/context browser matrix required by P13 across Chromium, Firefox and WebKit, including strict empty `My work`, context switching, stale/inaccessible IDs and Projects error/recovery.

## Dependency direction

```text
Shell/Registry
  ├─ capability + lifecycle contracts
  ├─ auth/navigation/notification contracts
  └─ Projects capability
       ├─ ProjectListProvider        provides projects.list.read
       ├─ ProjectContext             provides projects.context.select
       ├─ ProjectToolbarExtensions   provides projects.toolbar.extend
       ├─ ProjectSelectedRecord      provides projects.selected-record.select
       ├─ ProjectWorkData            provides projects.work-data.service
       ├─ ProjectEvidence            provides projects.evidence.service
       ├─ ProjectScope
       ├─ ProjectHierarchy
       └─ ProjectInspector

Portfolio
  └─ requires projects.list.read + projects.context.select

Capacity / Research
  └─ require projects.context.select + projects.toolbar.extend
     (no concrete `projects.core` dependency)

Dossiers
  └─ requires projects.context.select
     (no concrete `projects.core`, list or toolbar dependency)
```

## Working order

Work A01 → A08 before treating feature extraction as certified. For the current Projects requirement, proceed P01 → P08 first, then P09 → P17 certification. Keep changes small: introduce/validate one contract boundary, migrate one consumer, prove removal/replacement behavior, then continue. Do not bundle unrelated parity repairs into an architecture slice.
