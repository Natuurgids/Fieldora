# Documentation Archive

This directory preserves historical evidence that is no longer current guidance.

- `releases/` contains build, phase, installation, and implementation summaries.
- `repairs/` contains historical code-review and corrective-build reports.
- `validation/` contains test summaries, field status, and certification snapshots.

Files are retained for provenance. Use `docs/DOCUMENTATION.md` for current guidance.
