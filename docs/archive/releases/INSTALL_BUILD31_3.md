# Install Aperture Build 31.3

Build 31.3 is a clean-install field-test release.

Windows: extract the Windows archive and run `Install Aperture.cmd`.

Linux: extract the Linux archive and run `bash "Install Aperture Linux.sh"`.

Create a new Aperture Library. Earlier development libraries are not migrated by this release.
