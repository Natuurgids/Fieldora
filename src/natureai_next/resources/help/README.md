# Fieldora Runtime Help

This directory is a generated runtime mirror used by the integrated Help browser.
Do not author documentation here. Edit the canonical root or `docs/` source and run:

```text
python scripts/sync_help_docs.py
```
